# Documentation static site generator & deployment tool
mkdocs >= 1.1.2, == 1.*

# Add your custom theme if not inside a theme_dir
# (https://github.com/mkdocs/mkdocs/wiki/MkDocs-Themes)
mkdocs-material >= 9.6.10, == 9.*

# Requirements for extras/b2input
beautifulsoup4 >= 4.11.1, == 4.*
lxml >= 4.9.2, == 4.*  # has new major version
pyyaml >= 6.0, == 6.*
# Fix broken dependency (current versions: mkdocs-1.2.3, mkdocs-material-8.2.6)
# by freezing jinja2 version
#jinja2==3.0.3
